import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const productListItems = () => {
  return (
    <View>
      <Text>ProductListItems</Text>
    </View>
  )
}

export default productListItems

const styles = StyleSheet.create({})